import { NextRequest, NextResponse } from 'next/server'
import { getCurrentAdmin } from '@/lib/internal/auth'
import { idOf, orderOf, statusOf, text } from '@/lib/internal/pawn'
import { listProducts, listVariants, updateProduct } from '@/lib/pawn-catalog-store'
export const runtime='nodejs'
export async function GET(_request:NextRequest,context:{params:Promise<{id:string}>}){if(!await getCurrentAdmin())return NextResponse.json({error:'Unauthorized'},{status:401});const {id}=await context.params;const productId=Number(id);if(!Number.isFinite(productId))return NextResponse.json({error:'Produk tidak ditemukan.'},{status:404});const product=listProducts().find(item=>item.id===productId);if(!product)return NextResponse.json({error:'Produk tidak ditemukan.'},{status:404});const variants=listVariants().filter(variant=>variant.product_id===productId);return NextResponse.json({data:{id:product.id,category_id:product.category_id,brand_id:product.brand_id,name:product.name,slug:product.slug,description:product.description,search_keywords:product.search_keywords,image_url:product.image_url,sort_order:product.sort_order,status:product.status,variants}})}
export async function PATCH(request:NextRequest,context:{params:Promise<{id:string}>}){const admin=await getCurrentAdmin();if(!admin)return NextResponse.json({error:'Unauthorized'},{status:401});const {id}=await context.params;if(!/^\d+$/.test(id))return NextResponse.json({error:'ID tidak valid.'},{status:400});const x=await request.json().catch(()=>({}));const categoryId=idOf(x.categoryId),brandId=idOf(x.brandId),name=text(x.name,180);if(!categoryId||!brandId||!name)return NextResponse.json({error:'Kategori, brand, dan nama produk wajib diisi.'},{status:400});const updated=updateProduct(Number(id),{categoryId,brandId,name,slug:text(x.slug,190)||undefined,description:text(x.description,65535)||undefined,searchKeywords:text(x.searchKeywords,65535)||undefined,imageUrl:text(x.imageUrl,1000)||undefined,sortOrder:orderOf(x.sortOrder),status:statusOf(x.status)});if(!updated)return NextResponse.json({error:'Produk atau referensi kategori/brand tidak ditemukan.'},{status:404});return NextResponse.json({ok:true})}
